export enum LessonTypes {
  Sori = "sori",
  Voca = "voca",
  Text = "text",
  Vid = "vid",
  Book = "book",
}

export enum Phases {
  Learn = "learn",
  Review = "review",
  Test = "test",
}

export enum BlockStepType {
  ww = "watch-whole",
  st = "sentence",
  rp = "role-play",
  // ADD MORE LATER
}

export enum SubBlockType{
  
}

export enum BlockStudyType {
  sp = "speakin",
  voc ="vocab",
  watch =''

  st = "sentence",
  
  vid = "video",
  doc = "document",
  cus = "custom",
  q = "question",
  mw = "mission-writing",
}
